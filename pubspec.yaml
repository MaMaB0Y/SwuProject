name: daibuddy
description: "A new Flutter project for DAIBUDDY."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  flutter_secure_storage: ^10.3.1
  crypto: ^3.0.7
  logger: ^2.7.0
  permission_handler: ^12.0.3
  flutter_phone_direct_caller: ^2.2.1
  flutter_local_notifications: ^22.1.0
  timezone: ^0.11.1
  flutter_timezone: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
